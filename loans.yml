server:
  port: 9020
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: ''
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

# application related properties
build:
  version: "3.0"

accounts:
  message: "Welcome to eazybanks Loans related local API."
  contactDetails:
    name: "development team"
    email: "dev.loans@abc.com"
  onCallSupport:
    - (555) 555-2345
    - (555) 555-4523


